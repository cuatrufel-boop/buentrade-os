# Copy this file to scripts/secrets/.env.local (already gitignored, never commit it) and fill in
# the real value. Find it at https://resend.com/api-keys (Resend dashboard — works even while the
# Netlify site itself is paused, since Resend is a separate service).
RESEND_API_KEY=
